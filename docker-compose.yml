version: '3'
services:
   shiny:
        container_name: airshiny
        build: .
        ports: 
            - "80:3838"
        volumes: 
            - /var/log/shiny /app:/logs
        restart: unless-stopped
  
