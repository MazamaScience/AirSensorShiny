version: '3'
services:
   shiny:
      container_name: airsensor-shiny-default
      image: airsensor-shiny-default
      build: .
      ports: 
          - "6709:3838"
      volumes: 
          - /var/log/airsensor-shiny/test/app:/app/logs
      restart: unless-stopped
  
networks:
  custom:
    driver: bridge
    ipam:
      driver: default
      config:
        - subnet: 172.67.09.0/24
          gateway: 172.67.09.1
